<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Drivewave</title>
    <meta name="description" content="">
    <meta name="author" content="">
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <link rel="stylesheet" href="http://code.jquery.com/mobile/1.0a3/jquery.mobile-1.0a3.min.css" />
    <link rel="stylesheet" href="stylesheets/custom.css" />
    <script src="http://code.jquery.com/jquery-1.5.min.js"></script>
    <script src="http://code.jquery.com/mobile/1.0a3/jquery.mobile-1.0a3.min.js"></script>
    <%#= stylesheet_link_tag :all %>
    <%#= javascript_include_tag :defaults %>
    <%#= csrf_meta_tag %>
    <style type="text/css" media="screen">
      /* Hide text-field*/
      #slider {
        display: none;
      }
      div.ui-slider {
        width: 90%;
      }
      label.ui-block-right{
        text-align: right;
        float: right;
      }
      .min-width-480px div.ui-slider {
        width: 90%;
      }
      .min-width-480px label.ui-slider {
        width: 50%;
      }
    </style>
  </head>
  <body>
    <div>
      <% if current_user? %>
        <p>You are accessed Smartphone.</p>
        <p><strong><%= current_user.name %></strong>さん&nbsp;
        <%= link_to '[ホーム]', root_path %>&nbsp;
        <%= link_to '[テーマ]', themes_path %>&nbsp;
        <%= link_to '[ログアウト]', logout_path %></p>
      <% else %>
        <%= link_to '[ログイン]', login_path %>
      <% end %>
    </div>

    <%= yield %>

  </body>
</html>
